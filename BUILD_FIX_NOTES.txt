Haruki NovaTube Android v0.8.2 — Build Fix Carry-Forward
========================================================

The v0.8.0 compile fixes and v0.8.1 performance fixes are retained in v0.8.2:

1. RecommendationStore.kt
   - topicSignature(BrowseVideo) helper remains present.

2. Mp3Transcoder.kt
   - Uses TAndroidLame 1.1 encodeBufferInterLeaved(...) and flush(...).
   - Native encoder is closed in finally.

3. app/build.gradle.kts
   - TAndroidLame 1.1 keeps its legacy com.android.support transitive dependencies excluded so AndroidX core/media do not collide.

4. v0.8.1 performance carry-forward
   - 8 MiB bounded video/audio chunks, playback-safe download progress, Shorts Watch restoration and Nova AI modes remain guarded.

5. v0.8.2 additions
   - Native per-session Equalizer, popular/custom presets, app-wide EQ playback paths and premium Compose interaction hooks are validated by tools/preflight.py.

Current project version: 0.8.2 / versionCode 820.
